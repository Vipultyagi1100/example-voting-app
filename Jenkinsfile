pipeline {
    agent any   {
       stage('Build') { 
        echo "build stage is running" 
    }
    stage('Test') { 
       echo "test stage is running" 
    }
    stage('Deploy') { 
        echo "deploy stage is running" 
    }
    }
}
